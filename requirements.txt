# Environment variable management
python-dotenv

streamlit>=1.0.0

# LangChain core and integrations
langchain==0.3.25
langchain-google-genai==2.1.4
langchain-community==0.3.24
# Removed langchain-experimental due to version conflicts. Add a newer, compatible version if essential.
langchain-huggingface

# Database driver for MySQL
pymysql>=1.1.0
sqlalchemy>=2.0.30

# Vector store and embeddings
numpy>=1.26.2
chromadb==0.4.24
sentence-transformers==2.7.0

# HuggingFace support (compatible with Rust on Streamlit Cloud)
transformers>=4.39.0
tokenizers>=0.19.1  # <--- CHANGED THIS LINE

# Torch for embeddings
torch>=2.1.0

# Jupyter and dev tools
notebook
ipykernel
tqdm

faiss-cpu
